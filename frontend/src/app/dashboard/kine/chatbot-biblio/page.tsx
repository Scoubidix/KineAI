// Ancienne page IA Bibliographique — fusionnée dans le chat unifié.
import { redirect } from 'next/navigation';

export default function LegacyChatbotBiblioRedirect() {
  redirect('/dashboard/kine/chat');
}
