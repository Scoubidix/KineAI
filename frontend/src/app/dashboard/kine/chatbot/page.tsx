// Ancienne page IA Conversationnelle — fusionnée dans le chat unifié.
import { redirect } from 'next/navigation';

export default function LegacyChatbotRedirect() {
  redirect('/dashboard/kine/chat');
}
