// src/app/shared/layout.tsx
// This file can be used for shared layout components if needed later.
// For now, it's empty as AppLayout handles the main structure.
import React from 'react';

export default function SharedLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
